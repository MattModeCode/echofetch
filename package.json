{
  "name": "echofetch",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that saves Echo360 lectures for offline viewing.",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
